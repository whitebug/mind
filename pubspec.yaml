name: mind
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  bloc: ^4.0.0
  flutter_bloc: ^4.0.0
  equatable: ^1.1.1
  meta: ^1.1.8
  shared_preferences: ^0.5.7
  audioplayers: ^0.15.1
  http: ^0.12.0+4
  path_provider: ^1.6.7
  path_provider_macos: ^0.0.4+1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
  assets:
    - assets/design/
